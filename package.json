{
  "name": "vue3-booking-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:e2e": "concurrently \"yarn dev\" \"wait-on http://localhost:3000 && npx playwright test\"",
    "test:e2e:ui": "concurrently \"yarn dev\" \"wait-on http://localhost:3000 && npx playwright test --ui\""
  },
  "dependencies": {
    "vue": "^3.5.23",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "concurrently": "^9.2.1",
    "vite": "^5.0.0",
    "wait-on": "^9.0.1"
  }
}
